# smart-chatbot-platform (Working MVP)
Uses sentence-transformers (all-MiniLM-L6-v2) for embedding local docs and semantic search.
Requirements: sentence-transformers, numpy, scikit-learn
Note: model will be downloaded at first run (~100MB).